import { StyleSheet } from 'react-native'

const TileWrapper = StyleSheet.create({
  tile: {
    borderColor: '#000000',
    borderWidth: 4,
    width: 100,
    height: 100,
  },
})

export default TileWrapper
